name: Windows CI

on:
  push:
    branches: [ main, master, develop ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master, develop ]

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up MSVC
        uses: microsoft/setup-msbuild@v2

      - name: Install NSIS
        shell: pwsh
        run: choco install nsis --no-progress -y

      - name: Configure CMake
        shell: pwsh
        run: |
          cmake -B build -S . -G "Visual Studio 17 2022" -A x64 `
            -DCMAKE_INSTALL_PREFIX="${{ github.workspace }}/stage"

      - name: Build
        shell: pwsh
        run: cmake --build build --config Release --parallel 4

      - name: Install
        shell: pwsh
        run: cmake --install build --config Release --prefix "${{ github.workspace }}/stage"

      - name: Generate CPack packages (NSIS, ZIP)
        shell: pwsh
        working-directory: build
        run: |
          $pkgDir = "${{ github.workspace }}/artifacts/packages"
          New-Item -ItemType Directory -Path $pkgDir -Force | Out-Null
          cpack -G NSIS -C Release -B $pkgDir --verbose
          cpack -G ZIP -C Release -B $pkgDir

      - name: Locate NSIS installer output
        shell: pwsh
        run: |
          $pkgDir = "${{ github.workspace }}/artifacts/packages"
          $nsis = Get-ChildItem -Path $pkgDir -Recurse -File -Filter *.exe
          if (-not $nsis) {
            Write-Warning "No NSIS installer found in artifacts/packages."
            Write-Host "EXE files found under artifacts/packages:"
            Get-ChildItem -Path $pkgDir -Recurse -File -Filter *.exe | ForEach-Object {
              Write-Host " - $($_.FullName)"
            }
          } else {
            $nsis | ForEach-Object { Write-Host "NSIS installer: $($_.FullName)" }
          }

      - name: Collect artifacts
        shell: pwsh
        run: |
          $pkgDir = "${{ github.workspace }}/artifacts/packages"
          Get-ChildItem -Path $pkgDir -Recurse -File | Where-Object {
            $_.Extension -in @('.exe', '.zip')
          } | ForEach-Object {
            Write-Host "Package: $($_.FullName)"
          }

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: vscpl1drv-can232-windows-packages
          path: artifacts/packages/
          retention-days: 30

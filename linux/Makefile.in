# =========================================================================
#                      Autogenerated from Makefile.in
# =========================================================================

# Package version
MAJOR_VERSION=@MAJOR_VERSION@
MINOR_VERSION=@MINOR_VERSION@
RELEASE_VERSION=@RELEASE_VERSION@
BUILD_VERSION=@BUILD_VERSION@
PACKAGE_VERSION=@PACKAGE_VERSION@

# STATIC = yes if a static build is requested.
STATIC=@STATIC@
VSCP_PROJ_BASE_DIR=@DESTDIR@
DESTDIR=@DESTDIR@

prefix = @prefix@
exec_prefix = @exec_prefix@
INSTALL = @INSTALL@
top_srcdir = @top_srcdir@
top_builddir = @top_builddir@

CC = @CC@
CXX = @CXX@

WARNINGS_HARD = -Werror -Wall -Wextra -pedantic -Wcast-align -Wcast-qual \
  -Wctor-dtor-privacy -Wdisabled-optimization -Wformat=2 -Winit-self \
  -Wlogical-op -Wmissing-include-dirs -Wnoexcept -Wold-style-cast \
  -Woverloaded-virtual -Wredundant-decls -Wshadow -Wsign-promo \
  -Wstrict-null-sentinel -Wstrict-overflow=5 -Wundef -Wno-unused \
  -Wno-variadic-macros -Wno-parentheses -fdiagnostics-show-option
WARNINGS_MEDIUM = -Wformat-security -Wformat=2 -Wmissing-include-dirs \
  -Wno-parentheses -Wformat=2 -Wlogical-op
WARNINGS = -Wall

VERSION_DEFS = -DMAJOR_VERSION=$(MAJOR_VERSION) -DMINOR_VERSION=$(MINOR_VERSION) \
	-DRELEASE_VERSION=$(RELEASE_VERSION) -DBUILD_VERSION=$(BUILD_VERSION)

CFLAGS = @CFLAGS@ -fPIC ${WARNINGS} -DCBC \
	-I../vscp/src/vscp/common \
	-I../vscp/src/common
CXXFLAGS = -D__LINUX__ -fPIC ${WARNINGS} -D_REENTRANT @CXXFLAGS@
CPPFLAGS = -D__LINUX__ -fPIC ${WARNINGS} @CPPFLAGS@ \
	-fno-var-tracking-assignments \
	-I../vscp/src/vscp/common \
	-I../vscp/src/common

LDFLAGS = @LDFLAGS@
EXTRALIBS = @EXTRALIBS@

DLFLAGS = -g -shared -fPIC -Wl,--no-undefined
STRIP = strip
INSTALL_PROGRAM = ${INSTALL}
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_DIR = /usr/bin/install -c -d
srcdir = .
top_srcdir = .
top_builddir =
bindir = ${exec_prefix}/bin
libdir = ${exec_prefix}/lib
datadir = ${prefix}/share
includedir = ${prefix}/include
DLLPREFIX = lib
LIB_SONAME = "vscpl1drv-can232"

LIB_PLUS_VER = "vscpl1drv-can232.so.$(MAJOR_VERSION).$(MINOR_VERSION).$(RELEASE_VERSION)"
LIB_SONAME = "vscpl1drv-can232.so.$(MAJOR_VERSION)"

### Variables: ###

CAN232DRV_OBJECTS = vscpl1drv-can232.o\
	can232obj.o\
	dllist.o\
	com.o

### Targets: ###

all: $(LIB_PLUS_VER) ../man/vscpl1drv-can232.1

test:  test.o $(PROJOBJS)
	$(CXX) -o $@ test.o $(PROJOBJS) $(LDFLAGS) -L$(LIBDIRNAME) @LIBS@ $(EXTRALIBS)

$(LIB_PLUS_VER): $(CAN232DRV_OBJECTS)
	$(CXX) -Wl,-soname,$(LIB_SONAME) -o $@ $(CAN232DRV_OBJECTS) $(DLFLAGS) -L$(LIBDIRNAME) @LIBS@ $(EXTRALIBS)
	ar rcs libvscpl1drv-can232.a $(VSCPHELPER_OBJECTS)

can232obj.o: can232obj.cpp can232obj.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c can232obj.cpp -o $@

guid.o: ../vscp/src/vscp/common/guid.cpp ../vscp/src/vscp/common/guid.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../vscp/src/vscp/common/guid.cpp -o $@

dllist.o: ../vscp/src/common/dllist.c ../vscp/src/common/dllist.h
	$(CC) $(CFLAGS) -c ../vscp/src/common/dllist.c -o $@

com.o: ../vscp/src/common/com.cpp ../vscp/src/common/com.h
	$(CXX) $(CXXFLAGS) $(CPPFLAGS) -c ../vscp/src/common/com.cpp -o $@

../man/vscpl1drv-can232.1: ../man/vscpl1drv-can232.1.md
	pandoc -s -t man ../man/vscpl1drv-can232.1.md -o ../man/vscpl1drv-can232.1

man: ../man/vscpl1drv-can232.1.md
	pandoc -s -t man ../man/vscpl1drv-can232.1.md -o ../man/vscpl1drv-can232.1

install: all
	$(INSTALL_DIR) $(DESTDIR)$(libdir)
	$(STRIP) $(LIB_PLUS_VER)
	$(INSTALL_PROGRAM) $(LIB_PLUS_VER) $(DESTDIR)$(libdir)
	ln -fs $(libdir)/$(LIB_PLUS_VER) $(DESTDIR)$(libdir)/vscpl1drv-can232.so
	ln -fs $(libdir)/$(LIB_PLUS_VER) $(DESTDIR)$(libdir)/vscpl1drv-can232.so.$(LIB_MAJOR_VERSION)
	ln -fs $(libdir)/$(LIB_PLUS_VER) $(DESTDIR)$(libdir)/vscpl1drv-can232.so.$(LIB_MAJOR_VERSION).$(LIB_MINOR_VERSION)
	$(INSTALL_PROGRAM) libvscpl1drv-can232.a $(DESTDIR)$(libdir)
	pandoc -s -t man ../man/vscpl1drv-can232.1.md -o ../man/vscpl1drv-can232.1
	$(INSTALL_PROGRAM) ../man/vscpl1drv-can232.1 /usr/share/man/man1/
	mandb

uninstall:
	rm -f  $(DESTDIR)$(libdir)/libvscpl1drv-can232.a
	rm -f  $(DESTDIR)$(libdir)/$(LIB_PLUS_VER)
	rm -f  $(DESTDIR)$(libdir)/vscpl1drv-can232.so.$(LIB_MAJOR_VERSION)
	rm -f  $(DESTDIR)$(libdir)/vscpl1drv-can232.so.$(LIB_MAJOR_VERSION).$(LIB_MINOR_VERSION)

install-strip: install

deb:    all
	echo "\n" | ./builddist.sh
	@mkdir -p ../../../../../../dist
	@mv *.deb ../../../../../../dist

clean:
	rm -rf ./.deps ./.pch
	rm -f ./*.o
	rm -f ../common/*.o
	rm -f ../vscp/src/vscp/common/*.o
	rm -f ../vscp/src/common/*.o
	rm -f vscpl1drv-can232.so
	rm -f test
	rm -rf ../common/*~
	rm -rf *~
	rm -f *.gz
	rm -f *.deb

distclean: clean
	rm -f config.cache config.log config.status bk-deps bk-make-pch shared-ld-sh

$(ALWAYS_BUILD):  .FORCE

.FORCE:

# Include dependency info, if present:
-include .deps/*.d

.PHONY: all install uninstall clean distclean data .FORCE
